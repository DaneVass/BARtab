params {
    config_profile_name        = "Test single-end bulk profile"
    config_profile_description = "Minimal test dataset to check single-end bulk workflow"

    input                      = "$baseDir/test/samplesheets/single-bulk-index.csv"
    mode                       = "single-bulk"
    outdir                     = "$baseDir/test/test_out/single_end_index/"
    max_cpus                   = 6
    max_memory                 = "8.GB"
    max_time                   = "2.h"
}